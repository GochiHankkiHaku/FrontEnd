export const STORAGE = {
  jwt: 'jwt',
} as const;

export const PATH = {
  onBoarding: 'onboarding',
  location: 'location',
  schedule: 'schedule',
  memberCount: 'membercount',
  cook: 'cook',
  cost: 'cost',
  contact: 'contact',
  main: 'main',
  recheck: 'recheck',
} as const;

export const MAXWIDTH = 400;
