export const STORAGE = {
  jwt: 'jwt',
} as const;

export const PATH = {
  login: 'login',
  dashboard: 'dashboard',
  userList: 'userlist',
  bodyStatus: 'bodystatus',
} as const;

export const MAXWIDTH = 600;
